import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-menu',
  templateUrl: './menu.component.html',
  styleUrls: ['./menu.component.css']
})
export class MenuComponent implements OnInit {

  constructor() { }

  ngOnInit(): void {
    document.addEventListener('DOMContentLoaded', function() {
      const menuppal = document.getElementById('menuppal');
    
      // Asumiendo que `meanmenu` es una función que maneja la lógica del menú
      document.getElementById('menuppal')
    
      // meanScreenWidth es una opción, así que debería pasarla dentro del objeto de opciones.
      document.getElementById('menuppal')
        meanScreenWidth: "768"
    });

  }

}
